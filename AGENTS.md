# 项目规范

## 1. 回答规范（强制遵守）

每次回复我消息时必须遵守以下的格式

- 做了什么

- 有什么影响

- 待确认的事

- 下一步建议做什么

- 先给出结论，再说明原因、影响范围和验证结果

- 引用代码时注明文件路径、函数名或符号名。

- 区分“已从代码确认”“基于文档确认”和“尚待硬件验证”。

- 不确定的信息必须明确说明，不猜测寄存器含义、硬件行为或协议时序。

- \# 八荣八耻

  1.以暗猜接口为耻，以认真查阅为荣

  2.以模糊执行为耻，以寻求确认为荣

  3.以盲想业务为耻，以人类确认为荣

  4.以创造接口为耻，以复用现有为荣

  5.以跳过验证为耻，以主动测试为荣

  6.以破坏架构为耻，以遵循规范为荣

  7.以假装理解为耻，以诚实无知为荣

  8.以盲目修改为耻，以谨慎重构为荣

## 2. 修改原则

- 本项目仓库内的文本源码和文档统一使用UTF-8编码。
- 修改前先阅读相关实现、调用关系、配置文件和现有代码风格。
- 优先采用最小范围修改，避免无关重构和批量格式化。
- 保留用户已有改动，不覆盖、不回退与当前任务无关的修改。
- 不随意更改第三方库、自动生成文件、启动代码、链接脚本和板级配置。
- 如果必须修改上述内容，先说明必要性、影响范围和恢复方式。
- 新增代码应沿用现有命名、缩进、类型定义、错误处理和注释风格。
- 不为消除警告而使用不安全的强制类型转换或屏蔽诊断。
- 不引入动态内存、阻塞等待或长临界区，除非明确说明原因并评估影响。

## 3.. 嵌入式 C 检查要求

- 检查整数宽度、符号扩展、溢出、对齐、字节序和结构体填充。
- 涉及寄存器或并发访问时，正确使用 `volatile`，但不得用它替代同步机制。
- 涉及中断、任务、回调、DMA 或多核共享数据时，检查竞态、原子性和可见性。
- 涉及 DMA 或缓存时，检查缓冲区对齐、生命周期以及 cache clean/invalidate。
- 避免在中断服务程序中执行阻塞操作、复杂日志或不可重入函数。
- 检查数组边界、指针有效性、资源释放和错误路径。
- 寄存器修改应尽量注明寄存器名称、位定义和依据。

## 4. 构建与验证

- 修改完成后执行与改动范围相匹配的构建或检查。
- 不把“代码看起来正确”描述为“编译通过”或“测试通过”。
- 报告实际执行的命令、结果、错误和新增警告。
- 如果无法构建，应说明阻碍原因并完成可行的静态检查。
- 如果无法进行目标板验证，应列出具体的板级验证步骤和预期现象。
- 涉及实时性或吞吐量时，应说明需要采集的时间、速率和丢包指标。

## 5. 生成文件和函数

- 新建或修改文本文件后必须使用UTF-8严格解码验证，不得提交混合编码文件.
- 函数需要表明函数本身，返回值，参数等作用。
